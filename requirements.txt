# Core Dependencies
onnxruntime-gpu>=1.17.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
opencv-python>=4.9.0
pillow>=10.2.0
numpy>=1.26.0
python-multipart>=0.0.9

# RF-DETR Dependencies (para exportación del modelo)
torch>=2.0.0
torchvision>=0.15.0
# Instalar RF-DETR desde GitHub: pip install git+https://github.com/roboflow/rf-detr.git
onnx>=1.15.0
onnxsim>=0.4.0

# Voice narrator — edge-tts (neural, Python 3.14-compatible) + pyttsx3 (fallback)
# Playback usa Windows MCI via ctypes — sin dependencias extra
edge-tts>=6.1.9
pyttsx3>=2.90
